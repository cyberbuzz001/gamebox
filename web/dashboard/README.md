# Dashboard

The HTML report at `data/reports/report.html` is the v1 dashboard: security
score, endpoint/finding counts, and a filterable findings table. Open it in a
browser after running `gamebox report`. A live web UI can be added here later.
